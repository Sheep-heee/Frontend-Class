import React from "react";

// const x = ["a", "b", "c", "d", "e"];

// x.splice(0, 1);

// x.splice(2, 0, "a");

const toDos = {
  x: ["a", "b"],
  y: ["c", "d"],
};

Object.keys(toDos);

Object.values(toDos);
